name: bloc_notas
description: "BlocNotas - App de ejemplo en Flutter que demuestra Drawer, NavigationDrawer, BottomNavigationBar, PopupMenuButton y menús desplegables (Dropdown)."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
